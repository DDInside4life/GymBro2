<Window x:Class="GymBro.App.Views.ProgramSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор программы тренировок" 
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выберите программу из списка" 
                   FontSize="18" FontWeight="SemiBold" Margin="20,20,20,10"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Templates}" 
                 SelectedItem="{Binding SelectedTemplate}"
                 Margin="20,0,20,10"
                 Style="{StaticResource CardListBoxStyle}"
                 ItemContainerStyle="{StaticResource CardListItemStyle}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#7F8C8D"/>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding ProgramType}" 
                                   FontSize="12" Foreground="#3498DB" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
            <Button Content="Выбрать" Command="{Binding SelectCommand}" 
                    Background="#3498DB" Foreground="White" Padding="15,8" Margin="5" Width="100"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" 
                    Background="#95A5A6" Foreground="White" Padding="15,8" Margin="5" Width="100"/>
        </StackPanel>
    </Grid>
</Window>