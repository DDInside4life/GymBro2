<Page x:Class="GymBro.App.Pages.UsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GymBro.App.ViewModels"
      Title="Пользователи">
    <Page.DataContext>
        <vm:UsersPageViewModel />
    </Page.DataContext>

    <Page.Resources>
        <Style x:Key="AdminButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAdmin}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Пользователи" Style="{StaticResource Heading1Style}"/>

                <!-- Индикатор загрузки -->
                <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Загрузка..." HorizontalAlignment="Center" FontSize="16"/>
                </Border>

                <!-- Список пользователей -->
                <Border Background="White" CornerRadius="15" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="📋 Список пользователей" 
                                   FontSize="18" 
                                   FontWeight="SemiBold" 
                                   Foreground="#2C3E50" 
                                   Margin="0,0,0,15"/>

                        <ListBox Grid.Row="1" 
                                 ItemsSource="{Binding Users}" 
                                 SelectedItem="{Binding SelectedUser}"
                                 Style="{StaticResource CardListBoxStyle}"
                                 ItemContainerStyle="{StaticResource CardListItemStyle}"
                                 Height="400">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding FullName}" FontSize="16" FontWeight="SemiBold"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Login}" FontSize="12" Foreground="#7F8C8D" Margin="0,0,10,0"/>
                                                <TextBlock Text="{Binding Email}" FontSize="12" Foreground="#7F8C8D"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <TextBlock Text="{Binding Roles.Count, StringFormat='Ролей: {0}'}" 
                                                       FontSize="12" Foreground="#3498DB" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right" 
                                    Margin="0,15,0,0">
                            <Button Content="➕ Добавить" 
                                    Command="{Binding AddUserCommand}" 
                                    Style="{StaticResource AdminButtonStyle}"
                                    Padding="15,8"/>
                            <Button Content="✏️ Редактировать" 
                                    Command="{Binding EditUserCommand}" 
                                    Style="{StaticResource AdminButtonStyle}"
                                    Padding="15,8" Margin="10,0,0,0"/>
                            <Button Content="🗑 Удалить" 
                                    Command="{Binding DeleteUserCommand}" 
                                    Style="{StaticResource AdminButtonStyle}"
                                    Padding="15,8" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>