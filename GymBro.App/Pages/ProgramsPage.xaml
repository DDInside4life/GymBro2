<Page x:Class="GymBro.App.Pages.ProgramsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GymBro.App.ViewModels"
      xmlns:converters="clr-namespace:GymBro.App.Converters"
      Title="Программы">
    <Page.DataContext>
        <vm:ProgramsPageViewModel />
    </Page.DataContext>

    <Page.Resources>
        <converters:DifficultyConverter x:Key="DifficultyConverter"/>
        <converters:EquipmentToStringConverter x:Key="EquipmentToStringConverter"/>
        <Style x:Key="AdminButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsAdmin}" Value="False">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="#F5F7FA">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <TextBlock Text="Программы тренировок" Style="{StaticResource Heading1Style}"/>

                <!-- Индикатор загрузки -->
                <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Загрузка..." HorizontalAlignment="Center" FontSize="16"/>
                </Border>

                <!-- Список программ -->
                <Border Background="White" CornerRadius="15" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="📋 Доступные программы" 
                                   FontSize="18" 
                                   FontWeight="SemiBold" 
                                   Foreground="#2C3E50" 
                                   Margin="0,0,0,15"/>

                        <ListBox Grid.Row="1" 
                                 ItemsSource="{Binding Programs}" 
                                 SelectedItem="{Binding SelectedProgram}"
                                 Style="{StaticResource CardListBoxStyle}"
                                 ItemContainerStyle="{StaticResource CardListItemStyle}"
                                 Height="400">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Название и тип -->
                                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                                   Text="{Binding Name}" 
                                                   FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Grid.Row="0" Grid.Column="1" 
                                                   Text="{Binding ProgramType}" 
                                                   FontSize="12" Foreground="#3498DB" 
                                                   VerticalAlignment="Center" Margin="5,0,0,0"/>

                                        <!-- Описание и сложность -->
                                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="{Binding Description}" 
                                                       FontSize="12" Foreground="#7F8C8D" 
                                                       TextWrapping="Wrap" MaxWidth="300"/>
                                            <TextBlock Text="{Binding Difficulty, Converter={StaticResource DifficultyConverter}}" 
                                                       FontSize="12" Foreground="Orange" Margin="10,0,0,0"/>
                                        </StackPanel>

                                        <!-- Упражнения и оборудование -->
                                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,0,0">
                                            <ItemsControl ItemsSource="{Binding Exercises}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Padding="5,2" Margin="0,2" Background="#F8F9FA" CornerRadius="5">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" 
                                                                           Text="{Binding Name}" 
                                                                           FontSize="12" FontWeight="SemiBold"/>
                                                                <TextBlock Grid.Column="1" 
                                                                           Text="{Binding Equipment, Converter={StaticResource EquipmentToStringConverter}}" 
                                                                           FontSize="10" Foreground="#7F8C8D" Margin="5,0,0,0"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Кнопки управления -->
                        <StackPanel Grid.Row="2" 
                                    Orientation="Horizontal" 
                                    HorizontalAlignment="Right" 
                                    Margin="0,15,0,0">
                            <Button Content="✓ Выбрать" 
                                    Command="{Binding SelectProgramCommand}" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="15,8"/>
                            <Button Content="➕ Создать" 
                                    Command="{Binding CreateProgramCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="15,8" Margin="10,0,0,0"/>
                            <Button Content="✏️ Редактировать" 
                                    Command="{Binding EditProgramCommand}" 
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="15,8" Margin="10,0,0,0"/>
                            <Button Content="🗑 Удалить" 
                                    Command="{Binding DeleteProgramCommand}" 
                                    Style="{StaticResource AdminButtonStyle}"
                                    Padding="15,8" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>